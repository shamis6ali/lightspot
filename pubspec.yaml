name: lightspot_v1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # -- map & clustering
  google_maps_flutter: ^2.12.0          # ← pinned, no ^
  google_maps_cluster_manager_2: ^3.2.0

  # -- ui
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.8.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/pins/location-pin.png # your red pin
    - assets/pins/2.0x/location-pin.png
    - assets/pins/3.0x/location-pin.png